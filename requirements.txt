# Streamlit Community Cloud reads this file. The app (streamlit_app.py) reads the
# committed reports/*/replay_result.json directly AND drives the real replay engine
# under src/policy_replay/ in its interactive section. The trailing `.` installs this
# repo as a package so `from policy_replay.sim import replay` works on the Cloud.
streamlit>=1.40
.
