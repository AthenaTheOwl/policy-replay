# Streamlit Community Cloud reads this file. The app (streamlit_app.py) reads the
# committed reports/*/replay_result.json directly, so it only needs streamlit. The
# CLI/package under src/policy_replay/ has its own deps in pyproject.toml.
streamlit>=1.40
