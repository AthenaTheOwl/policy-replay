# design - 0002-design

## shape

The v0.1 loop loads a JSON rule, applies parameter overrides to a
baseline simulation parameter map, replays checked-in PJM RPM fixture
history, and writes a deterministic result record.

## non-goals

- Monte Carlo trials
- live FERC or ISO fetch
- offer-curve simulation
- recommendations

